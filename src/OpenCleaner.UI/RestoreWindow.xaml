<Window x:Class="OpenCleaner.UI.RestoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OpenCleaner.UI"
        Title="Restauration des fichiers" Height="560" Width="820"
        WindowStartupLocation="CenterOwner"
        x:Name="RestoreRoot">

    <Window.Resources>

        <!-- Reprend les mêmes couleurs que MainWindow via Application.Resources -->
        <SolidColorBrush x:Key="BgMain"      Color="#1C1C1C"/>
        <SolidColorBrush x:Key="BgCard"      Color="#252525"/>
        <SolidColorBrush x:Key="BgHover"     Color="#2D2D2D"/>
        <SolidColorBrush x:Key="BgSelected"  Color="#0078D4"/>
        <SolidColorBrush x:Key="Accent"      Color="#0078D4"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecond"  Color="#ABABAB"/>
        <SolidColorBrush x:Key="Border"      Color="#333333"/>

        <!-- Bouton Accent -->
        <Style x:Key="AccentBtnStyle" TargetType="Button">
            <Setter Property="Background"      Value="{DynamicResource Accent}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="20,10"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#1084D9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bg" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Bouton Secondaire -->
        <Style x:Key="SecondBtnStyle" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="18,10"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background"
                                        Value="{DynamicResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bg" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Bouton Danger -->
        <Style x:Key="DangerBtnStyle" TargetType="Button" BasedOn="{StaticResource SecondBtnStyle}">
            <Setter Property="Foreground"  Value="#D13438"/>
            <Setter Property="BorderBrush" Value="#D13438"/>
        </Style>

        <!-- ListViewItem -->
        <Style x:Key="RestoreItemStyle" TargetType="ListViewItem">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="8,7"/>
            <Setter Property="Margin"          Value="0,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background"
                                        Value="{DynamicResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bg" Property="Background"
                                        Value="{DynamicResource BgSelected}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GridViewColumnHeader -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{DynamicResource TextSecond}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="8,6"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
        </Style>

    </Window.Resources>

    <Border Background="{DynamicResource BgMain}">
        <Grid Margin="24,20,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- EN-TÊTE -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="🛡️  Restauration"
                           FontSize="22" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock Text="Sélectionnez un backup pour restaurer le fichier à sa localisation d'origine."
                           FontSize="12"
                           Foreground="{DynamicResource TextSecond}"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- SÉPARATEUR -->
            <Border Grid.Row="1" Height="1"
                    Background="{DynamicResource Border}"
                    Margin="0,0,0,16"/>

            <!-- LISTE DES BACKUPS -->
            <Border Grid.Row="2"
                    Background="{DynamicResource BgCard}"
                    CornerRadius="10"
                    Padding="4">
                <ListView x:Name="BackupsListView"
                          Background="Transparent"
                          BorderThickness="0"
                          ItemContainerStyle="{StaticResource RestoreItemStyle}"
                          SelectionChanged="BackupsListView_SelectionChanged"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.View>
                        <GridView>

                            <!-- ICÔNE -->
                            <GridViewColumn Header=" " Width="34">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="📄" FontSize="14"
                                                   VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- DATE FORMAT "il y a X jours" -->
                            <GridViewColumn Header="Date" Width="130">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy HH:mm}}"
                                                       Foreground="{DynamicResource TextPrimary}"
                                                       FontSize="12"/>
                                            <TextBlock Text="{Binding CreatedAt, Converter={x:Static local:RelativeDateConverter.Instance}}"
                                                       Foreground="{DynamicResource TextSecond}"
                                                       FontSize="10"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- CHEMIN ORIGINAL -->
                            <GridViewColumn Header="Fichier original" Width="360">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding OriginalPath}"
                                                   Foreground="{DynamicResource TextPrimary}"
                                                   FontSize="12"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!-- TAILLE -->
                            <GridViewColumn Header="Taille" Width="90">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Size, Converter={x:Static local:BytesConverter.Instance}}"
                                                   Foreground="{DynamicResource TextSecond}"
                                                   FontSize="12"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>

            <!-- BARRE D'ACTIONS -->
            <Border Grid.Row="3"
                    Background="{DynamicResource BgCard}"
                    CornerRadius="10"
                    Padding="16,12"
                    Margin="0,12,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="RefreshBtn"
                            Content="🔄  Rafraîchir"
                            Style="{StaticResource SecondBtnStyle}"
                            Click="Refresh_Click"
                            Margin="0,0,10,0"/>
                    <Button x:Name="DeleteOldBtn"
                            Content="🗑️  Supprimer anciens (&gt; 30 j)"
                            Style="{StaticResource DangerBtnStyle}"
                            Click="DeleteOld_Click"
                            Margin="0,0,10,0"/>
                    <Button x:Name="RestoreBtn"
                            Content="↩️  Restaurer"
                            Style="{StaticResource AccentBtnStyle}"
                            Click="Restore_Click"
                            IsEnabled="False"/>
                </StackPanel>
            </Border>

        </Grid>
    </Border>
</Window>
